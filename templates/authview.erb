<%
lstr = ""
if !@community.nil? then
    lstr = 'authcommunity'
    ltype = @community
elsif !@user.nil? then
    lstr = 'authuser'
    ltype = @user
else
    lstr = 'authgroup'
    ltype = @group
end
lstr = "#{lstr} #{@types}"

if !@model.nil? && !@model.empty? then
    lstr = "#{lstr} -s #{@model}"
end

lstr = "#{lstr} #{ltype}"

if !@source.nil? && !@source.empty? then
    lstr = "#{lstr} #{@source}"
elsif !@level.nil? && !@level.empty? then
    lstr = "#{lstr} #{@level}"
end

unless @oid.empty? then
    lstr = "#{lstr} #{@oid}"
end

unless @view.empty? then
    lstr = "#{lstr} -V #{@view}"
end
-%>
<%= lstr %>
