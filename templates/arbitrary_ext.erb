<%
lstr = @ext_type

if !@miboid.empty? then
    if [ "exec", "sh", "extend" ].include?(@ext_type) then
        lstr = "#{lstr} #{@miboid}"
    else
        lstr = "#Malformed entry -> #{lstr}"
    end
end

lstr = "#{lstr} #{@name} #{@prog} #{@args}"
-%>
<%= lstr %>
